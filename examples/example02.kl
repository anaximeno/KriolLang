inpristan "stdio.h"

nteru soma(nteru n) {
    nteru acc = 0;

    nteru i;
    pa i = 0 ; i < n ; i = i + 1 {
        acc = acc + i;
    }

    divolvi acc;
}

nteru inisiu() {
    mostra("Soma ti 10 é igual a %d\n", soma(10));
}
