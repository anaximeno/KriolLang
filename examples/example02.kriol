nter n = 10;

fn soma(nter n) nter {
    nter acc = 0;

    dipoz nter i;
    pa i = 1 ; i < n ; i += 1 {
        acc = acc + i;
    }

    divolvi acc;
}

fn inisiu() {
    mostran(f"Soma di 1 ti {n} é igual a {soma(n)}.");
}
